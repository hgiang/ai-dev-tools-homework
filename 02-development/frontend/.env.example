# Where the FastAPI backend is served. This is the default, so the file is only
# needed when the backend runs somewhere else.
VITE_API_BASE_URL=http://localhost:8000/api

# Set to true to run the UI against the in-memory mock instead of the server.
VITE_USE_MOCK_API=false
